# ============================================================================
# CHEZMOI IGNORE FILE
# ============================================================================

# Bootstrap scripts - bleiben separat, werden manuell ausgeführt
*.sh
*.ps1
README.md
LICENSE


# macOS-only files (use Library/ path)
{{ if or (ne .chezmoi.os "darwin") (or (eq .osid "wsl") (eq .osid "linux")) -}}
Library/**
dot_aerospace.toml
dot_gvimrc
{{- end }}


# Windows-only files (use AppData/ path)
{{ if or (ne .chezmoi.os "windows") (eq .osid "wsl") (eq .osid "linux") -}}
AppData/**
Documents/WindowsPowerShell/**
{{- end }}


# VSCode & lf config paths differ per OS
# On macOS: use Library/, ignore .config/Code & AppData/
# On Linux/WSL: use .config/, ignore Library/ & AppData/
# On Windows: use AppData/, ignore .config/ & Library/
{{ if eq .chezmoi.os "darwin" -}}
.config/Code
.config/Code/**
{{- end }}
{{ if or (eq .chezmoi.os "windows") (eq .osid "windows") -}}
.config/Code
.config/Code/**
.config/lf
.config/lf/**
{{- end }}
{{ if or (eq .chezmoi.os "linux") (eq .osid "wsl") -}}
AppData/**
Library/**
{{- end }}
