# Documentation
# https://nikitabobko.github.io/AeroSpace/guide

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
after-startup-command = ['layout v_accordion', 'mode main']

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# You can set 0 to disable the padding feature
accordion-padding = 20

# Possible values: tiles|accordion
default-root-container-layout = 'accordion'

# Possible values: horizontal|vertical|auto
default-root-container-orientation = 'vertical'

key-mapping.preset = 'qwerty'

# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
on-focus-changed = ['move-mouse window-lazy-center']


[gaps]
inner.horizontal = 5
inner.vertical =   5
outer.left =       5
outer.right =      5
outer.bottom =     5
outer.top =        [{ monitor.main = 5 }, { monitor.secondary = 14 }, 5]


[mode.main.binding] # 'main' binding mode must be always presented

ctrl-alt-cmd-slash = ['mode service']

ctrl-alt-cmd-leftSquareBracket = 'workspace --no-stdin prev'
ctrl-alt-cmd-rightSquareBracket = 'workspace --no-stdin next'
ctrl-alt-cmd-minus = ['resize smart -200']
ctrl-alt-cmd-equal = ['resize smart +200']

ctrl-alt-cmd-h = ['exec-and-forget aerospace focus left']
#ctrl-alt-cmd-h = ['exec-and-forget aerospace focus left --boundaries-action fail || aerospace workspace --no-stdin prev']
ctrl-alt-cmd-j = 'focus --boundaries all-monitors-outer-frame --boundaries-action stop down'
ctrl-alt-cmd-k = 'focus --boundaries all-monitors-outer-frame --boundaries-action stop up'
ctrl-alt-cmd-l = ['exec-and-forget aerospace focus right']
#ctrl-alt-cmd-l = ['exec-and-forget aerospace focus right --boundaries-action fail || aerospace workspace --no-stdin next']

ctrl-alt-cmd-d = ['workspace-back-and-forth']
#ctrl-alt-cmd-d = ['focus-monitor --wrap-around next']

#ctrl-alt-cmd-i = ['exec-and-forget aerospace list-workspaces --monitor focused --empty no | aerospace workspace prev']
#ctrl-alt-cmd-o = ['exec-and-forget aerospace list-workspaces --monitor focused --empty no | aerospace workspace next']

ctrl-alt-cmd-1 = 'move-node-to-workspace 1 --focus-follows-window'
ctrl-alt-cmd-2 = 'move-node-to-workspace 2 --focus-follows-window'

ctrl-alt-cmd-shift-h = ['move left', 'mode main']
ctrl-alt-cmd-shift-j = ['move down', 'mode main']
ctrl-alt-cmd-shift-k = ['move up', 'mode main']
ctrl-alt-cmd-shift-l = ['move right', 'mode main']

ctrl-alt-cmd-space = 'layout tiles accordion'
ctrl-alt-cmd-shift-space = 'layout horizontal vertical'

ctrl-alt-cmd-w = 'layout floating tiling'

[mode.service.binding]
r = ['reload-config', 'mode main']
q = ['exec-and-forget killall AeroSpace && open -a AeroSpace', 'mode main']
e = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout

backspace = ['close-all-windows-but-current', 'mode main']

# Try basic move first, then workspace switch
ctrl-alt-cmd-h = ['exec-and-forget aerospace move left --boundaries all-monitors-outer-frame --boundaries-action fail || aerospace move-node-to-workspace --no-stdin prev --focus-follows-window']
ctrl-alt-cmd-j = ['exec-and-forget aerospace move down --boundaries all-monitors-outer-frame --boundaries-action fail || aerospace move-node-to-workspace 2 --focus-follows-window']
ctrl-alt-cmd-k = ['exec-and-forget aerospace move up --boundaries all-monitors-outer-frame --boundaries-action fail || aerospace move-node-to-workspace 1 --focus-follows-window']
ctrl-alt-cmd-l = ['exec-and-forget aerospace move right --boundaries all-monitors-outer-frame --boundaries-action fail || aerospace move-node-to-workspace --no-stdin next --focus-follows-window']

#return to main mode
esc = ['mode main']

# Window detection rules
[[on-window-detected]]
if.app-name-regex-substring = 'Blender'
if.window-title-regex-substring = 'Preferences'
run = 'layout floating'

#this are no commands, they are rules (main or secondary)
[workspace-to-monitor-force-assignment]
1 = 'main'
2 = 'secondary'
