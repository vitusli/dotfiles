# ~/.bash_profile - Login shell configuration

# Scoop zum PATH hinzufügen
export PATH="$HOME/scoop/shims:$PATH"

# Lädt .bashrc für interaktive Shells
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi
